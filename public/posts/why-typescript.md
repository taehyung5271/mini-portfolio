# TypeScript 왜 써야 할까?
## 타입 안정성
JavaScript는 런타임에 에러가 터지지만, TypeScript는 코드를 작성하는 시점에 잡아준다.

## 자동완성
타입이 있으면 에디터가 어떤 속성과 메서드를 쓸 수 있는지 알려준다. 생산성이 올라간다.

## 협업
다른 사람이 만든 함수의 파라미터 타입을 보면 사용법을 바로 알 수 있다. 문서 역할을 한다.